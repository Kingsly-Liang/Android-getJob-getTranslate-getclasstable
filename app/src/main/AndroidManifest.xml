<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.liangweibin.study">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="主界面">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SchoolLoginActivity" />
        <activity android:name=".TranslationActivity" />
        <activity android:name=".GetJobActivity" />
        <activity android:name=".GetJobFromNetActivity" />
        <activity android:name=".ShowJobActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SearchClassActivity" />
        <activity android:name=".SearchGradeActivity" />
        <activity android:name=".JisuanqiActivity"></activity>
    </application>

</manifest>